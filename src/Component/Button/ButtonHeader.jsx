import React from 'react';

const ButtonHeader = (props,children) => {
    return ( 
        <div >
            <text>{props.value}</text>  
        </div>
     );
}
 
export default ButtonHeader;